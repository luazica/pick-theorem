# Pick's Theorem

## About
Just a schoolar project about Pick's Theorem. 
It states that the area of any simple polygon, whose vertices are points on a grid with integer coordinates, can be calculated precisely by counting only the interior points and the points on the polygon's boundary, using the formula:
A = I + B/2 - 1